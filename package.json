{
  "name": "data",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build_client": "cd client && npm run build",
    "build_admin": "cd admin && npm run build",
    "gitpush": "git push ssh web",
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix admin && npm run build --prefix admin",
    "heroku-postbuild": "npm install --prefix client && npm run build --prefix client && npm install --prefix admin && npm run build --prefix admin"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tamlt2704/data.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/tamlt2704/data/issues"
  },
  "homepage": "https://github.com/tamlt2704/data#readme",
  "dependencies": {
    "express": "^4.17.1",
    "morgan": "^1.10.0",
    "multer": "^1.4.2",
    "node-fetch": "^2.6.0",
    "node-tesseract": "^0.2.7"
  }
}
